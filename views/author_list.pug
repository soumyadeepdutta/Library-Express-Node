extends layout

block content
  h1= title
  
  ul
    each author in author_list
      li 
        a(href=author.url) #{author.name} 
        |  (#{author.dob} - #{author.dod})
        //- |  fullname #{author.dob}

    else
      li There are no authors.