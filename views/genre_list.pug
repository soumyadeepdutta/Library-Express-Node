extends layout

block content
  h1= title
  
  ul
    each genre in genres
      li 
        a(href=genre.url) #{genre.name} 
        //- |  (#{author.dob} - #{author.dod})
        //- |  fullname #{author.dob}

    else
      li There are no genres.